
services:
  openbridge-mcp:
    build: .
    image: openbridge-mcp:local
    ports:
      - "8002:8000"
    volumes:
      - type: bind
        source: .env
        target: /app/.env
        read_only: true
    restart: unless-stopped
