[build-system]
requires = [ "setuptools>=45", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "openbridge-mcp"
version = "0.3.7"
description = "FastMCP server for Openbridge API integration"
requires-python = ">=3.13"
readme = "README.md"
dependencies = [ "fastmcp[code-mode,tasks]>=4,<5", "openai>=1.68,<3", "python-dotenv", "requests", "pydantic", "PyJWT",]

[project.optional-dependencies]
dev = [ "pytest>=7.0", "pytest-asyncio>=0.21", "responses>=0.23", "pytest-httpx>=0.21", "ruff==0.15.12", "jsonschema>=4.0",]

[tool.setuptools.packages.find]
where = [ ".",]
include = [ "src*",]
