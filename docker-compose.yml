
services:
  openbridge-mcp:
    image: openbridgeops/openbridge-mcp:latest
    ports:
      - "8000:8000"
    volumes:
      - type: bind
        source: .env
        target: /app/.env
        read_only: true
    restart: unless-stopped
